{% comment %}
  L&M WARRANTY ACTIVATION — V8.9 FRESH UI
  Shopify Customer Account / OTP.
  Customer matches purchase by brand, phone and purchase date.
{% endcomment %}

<script>
(function(){
  var src='https://cdn.shopify.com/storefront/web-components.js';

  if(
    !customElements.get('shopify-account') &&
    !document.querySelector('script[src="'+src+'"]')
  ){
    var s=document.createElement('script');
    s.src=src;
    s.async=true;
    document.head.appendChild(s);
  }
})();
</script>

<section class="lmwa-customer">

{% if customer %}

  <script id="lmwa-shopify-customer" type="application/json">
    {
      "id": {{ customer.id | json }},
      "email": {{ customer.email | json }},
      "firstName": {{ customer.first_name | json }},
      "lastName": {{ customer.last_name | json }}
    }
  </script>

  
  <div class="lmwa-c-card">
    <aside class="lmwa-c-side">
      <div class="lmwa-c-side-copy">
        <div class="lmwa-c-kicker">BẢO HÀNH CHÍNH HÃNG</div>
        <h1>Kích hoạt<br>bảo hành</h1>
        <p>
          Hoàn tất thông tin mua hàng để xác nhận hiệu lực bảo hành cho sản phẩm.
        </p>
      </div>

      <div class="lmwa-c-verified-account">
        <div>
          <span>Email đã xác thực</span>
          <strong>{{ customer.email | escape }}</strong>
        </div>
        <shopify-account menu="customer-account-main-menu"></shopify-account>
      </div>

      <div class="lmwa-c-progress">
        <div class="done">
          <span>01</span>
          <strong>Xác thực</strong>
        </div>
        <i></i>
        <div class="active" data-step="2">
          <span>02</span>
          <strong>Đối chiếu</strong>
        </div>
        <i></i>
        <div data-step="3">
          <span>03</span>
          <strong>Hoàn tất</strong>
        </div>
      </div>
    </aside>

    <main class="lmwa-c-main">
      <header class="lmwa-c-main-head">
        <span>THÔNG TIN KÍCH HOẠT</span>
        <h2>Đối chiếu giao dịch</h2>
        <p>
          Nhập đúng thông tin đã dùng khi mua hàng. Email xác thực sẽ được đối chiếu tự động.
        </p>
      </header>

      <form id="customer-form" novalidate>

      <label>
        <span>Thương hiệu *</span>

        <select id="a-brand" required>
          <option value="">Chọn thương hiệu</option>
          <option value="GUESS">GUESS</option>
          <option value="TED BAKER">TED BAKER</option>
          <option value="PHILIPP PLEIN">PHILIPP PLEIN</option>
          <option value="ADIDAS">ADIDAS</option>
          <option value="FURLA">FURLA</option>
          <option value="LOCMAN ITALY">LOCMAN ITALY</option>
          <option value="VERSUS BY VERSACE">VERSUS BY VERSACE</option>
          <option value="MISSONI">MISSONI</option>
        </select>

        <small>Chọn thương hiệu đúng với sản phẩm trên hóa đơn hoặc phiếu mua hàng.</small>
      </label>

      <label>
        <span>Số điện thoại *</span>

        <input
          id="a-phone"
          type="tel"
          inputmode="tel"
          autocomplete="tel"
          placeholder="Nhập số điện thoại khi mua hàng"
          required
        >
      </label>

      <label>
        <span>Ngày mua *</span>

        <input
          id="a-date"
          type="date"
          required
        >

        <small>Ngày mua cần trùng với thông tin được ghi nhận khi tạo hồ sơ bảo hành.</small>
      </label>

      <div class="lmwa-c-match-note">
        <strong>Điều kiện kích hoạt</strong>

        <span>
          Thương hiệu, số điện thoại, ngày mua và email tài khoản phải trùng khớp hồ sơ.
        </span>
      </div>

      <button id="a-submit" type="submit">
        KÍCH HOẠT BẢO HÀNH
      </button>
    </form>

      <div id="a-message" class="lmwa-c-message" hidden></div>
      <section id="a-result" class="lmwa-c-result" hidden></section>
    </main>
  </div>




{% else %}

  
  <div class="lmwa-c-card lmwa-login-card">
    <aside class="lmwa-c-side">
      <div class="lmwa-c-side-copy">
        <div class="lmwa-c-kicker">BẢO HÀNH CHÍNH HÃNG</div>
        <h1>Kích hoạt<br>bảo hành</h1>
        <p>
          Xác thực email để bắt đầu quá trình kích hoạt bảo hành online.
        </p>
      </div>

      <div class="lmwa-c-login-note">
        <span>01</span>
        <div>
          <strong>Xác thực email</strong>
          <small>Mã xác thực được gửi trực tiếp đến email đăng ký.</small>
        </div>
      </div>
    </aside>

    <main class="lmwa-c-main lmwa-c-main-login">
      <div class="lmwa-shopify-auth">
        <span class="lmwa-c-main-kicker">BẮT ĐẦU</span>
        <h2>Xác thực tài khoản khách hàng</h2>
        <p>
          Sau khi xác thực thành công, trang sẽ tự tải lại để bạn tiếp tục nhập thông tin mua hàng.
        </p>

        <shopify-account
          id="lmwa-customer-account"
          menu="customer-account-main-menu"
        >
          <button
            type="button"
            slot="signed-out-avatar"
            class="lmwa-shopify-login-button"
          >
            GỬI MÃ XÁC THỰC
          </button>
        </shopify-account>
      </div>
    </main>
  </div>


  <script>
  (function(){
    function bindShopifyAccount(){
      var account=document.getElementById('lmwa-customer-account');

      if(!account || account.dataset.bound==='1') return;

      account.dataset.bound='1';

      account.addEventListener('close',function(){
        setTimeout(function(){
          window.location.reload();
        },850);
      });
    }

    if(customElements.get('shopify-account')){
      bindShopifyAccount();
    }else{
      customElements
        .whenDefined('shopify-account')
        .then(bindShopifyAccount)
        .catch(function(){});
    }
  })();
  </script>

{% endif %}

</section>

{% if customer %}

<script>
(function(){
  /* Production API */
  const API_BASE = 'https://lm-warranty-active.vercel.app/api/warranty';

  const customerData=JSON.parse(
    document.getElementById('lmwa-shopify-customer').textContent||'{}'
  );

  const form=document.getElementById('customer-form');
  const submit=document.getElementById('a-submit');
  const message=document.getElementById('a-message');
  const result=document.getElementById('a-result');

  const brand=document.getElementById('a-brand');
  const phone=document.getElementById('a-phone');
  const purchaseDate=document.getElementById('a-date');

  const normalizePhone=(value)=>
    String(value||'').replace(/[^\d+]/g,'').trim();

  const esc=(value)=>{
    const div=document.createElement('div');
    div.textContent=value==null?'':String(value);
    return div.innerHTML;
  };

  const formatDateOnly=(value)=>{
    const match=/^(\d{4})-(\d{2})-(\d{2})$/.exec(String(value||'').trim());
    if(!match) return '';
    return `${match[3]}/${match[2]}/${match[1]}`;
  };

  function show(type,text){
    message.hidden=false;
    message.className='lmwa-c-message '+type;
    message.textContent=text;
  }

  function hide(){
    message.hidden=true;
    message.textContent='';
  }

  function setFinalStep(){
    var steps=document.querySelectorAll('[data-step]');

    steps.forEach(function(step){
      step.classList.remove('active');
    });

    var finalStep=document.querySelector('[data-step="3"]');

    if(finalStep){
      finalStep.classList.add('active');
    }
  }

  form.addEventListener('submit',async function(event){
    event.preventDefault();

    hide();
    result.hidden=true;

    const payload={
      brand:String(brand.value||'').trim(),
      phone:normalizePhone(phone.value),
      purchaseDate:String(purchaseDate.value||'').trim(),
      shopifyEmail:String(customerData.email||'').trim().toLowerCase()
    };

    if(!payload.shopifyEmail){
      return show(
        'error',
        'Không xác định được email tài khoản Shopify. Vui lòng đăng nhập lại.'
      );
    }

    if(!payload.brand){
      return show('error','Vui lòng chọn thương hiệu.');
    }

    if(payload.phone.length<9 || payload.phone.length>15){
      return show('error','Số điện thoại chưa hợp lệ.');
    }

    if(!/^\d{4}-\d{2}-\d{2}$/.test(payload.purchaseDate)){
      return show('error','Vui lòng chọn ngày mua.');
    }

    const original=submit.textContent;

    submit.disabled=true;
    submit.textContent='ĐANG ĐỐI CHIẾU';

    try{
      const response=await fetch(
        API_BASE+'/customer/activate',
        {
          method:'POST',
          cache:'no-store',
          headers:{
            Accept:'application/json',
            'Content-Type':'application/json'
          },
          body:JSON.stringify(payload)
        }
      );

      const data=await response.json().catch(function(){
        return {};
      });

      if(!response.ok){
        throw new Error(
          data.error||
          'Không tìm thấy phiếu bảo hành phù hợp với thông tin đã cung cấp.'
        );
      }

      form.hidden=true;
      hide();
      setFinalStep();

      result.hidden=false;

      const alreadyActivated=Boolean(data.alreadyActivated);

      const activatedDate=data.activatedAt
        ?new Date(data.activatedAt).toLocaleDateString('vi-VN')
        :'';

      const warrantyExpiryDate=formatDateOnly(data.warrantyExpiresOn);

      result.innerHTML=`
        <div class="lmwa-c-kicker">
          ${alreadyActivated?'BẢO HÀNH ĐÃ CÓ HIỆU LỰC':'KÍCH HOẠT HOÀN TẤT'}
        </div>

        <h2>
          ${alreadyActivated
            ?'Phiếu bảo hành đã được kích hoạt trước đó'
            :'Bảo hành đã được kích hoạt'}
        </h2>

        <p>
          ${alreadyActivated
            ?'Thông tin mua hàng đã được đối chiếu thành công. Phiếu này đã có hiệu lực và không cần kích hoạt lại.'
            :'Thông tin mua hàng đã được đối chiếu thành công. Bảo hành online của sản phẩm hiện đã có hiệu lực.'}
        </p>

        <div class="lmwa-result-grid">
          <div>
            <span>Số bảo hành</span>
            <strong>${esc(data.warrantyNumber||'—')}</strong>
          </div>

          <div>
            <span>Thương hiệu</span>
            <strong>${esc(data.brand||payload.brand)}</strong>
          </div>

          <div>
            <span>SKU</span>
            <strong>${esc(data.sku||'—')}</strong>
          </div>

          <div>
            <span>Email khách hàng</span>
            <strong>${esc(customerData.email||'—')}</strong>
          </div>

          <div>
            <span>Hết hạn bảo hành</span>
            <strong>${esc(warrantyExpiryDate||'—')}</strong>
          </div>

          ${alreadyActivated&&activatedDate?`
          <div>
            <span>Ngày kích hoạt</span>
            <strong>${esc(activatedDate)}</strong>
          </div>
          `:''}
        </div>
      `;

    }catch(error){
      console.error(error);

      show(
        'error',
        error.message||
        'Không thể kích hoạt bảo hành với thông tin đã cung cấp.'
      );

    }finally{
      submit.disabled=false;
      submit.textContent=original;
    }
  });
})();
</script>

{% endif %}





<style>
/* ==========================================================
   L&M WARRANTY — FRESH CUSTOMER UI
   ========================================================== */

.lmwa-customer{
  --ink:#172019;
  --text:#303a33;
  --muted:#778078;
  --forest:#23432f;
  --forest-2:#305a3d;
  --forest-3:#426e4e;
  --sage:#e9f0ea;
  --ivory:#f4f3ee;
  --paper:#fff;
  --line:#d7ded8;
  --line-strong:#b8c4ba;
  --success:#2d5c3c;
  --success-bg:#e9f2eb;
  --danger:#8b4945;
  --danger-bg:#f8eeee;

  box-sizing:border-box;
  width:100%;
  padding:46px 18px 72px;
  background:
    radial-gradient(circle at 10% 0%,rgba(35,67,47,.08),transparent 27%),
    var(--ivory);
  color:var(--text);
  font-family:inherit;
  -webkit-font-smoothing:antialiased;
  text-rendering:optimizeLegibility;
}

.lmwa-customer *,
.lmwa-customer *:before,
.lmwa-customer *:after{box-sizing:border-box}

.lmwa-customer [hidden]{display:none!important}

.lmwa-customer button,
.lmwa-customer input,
.lmwa-customer select{font:inherit}

.lmwa-c-card{
  display:grid;
  grid-template-columns:minmax(280px,39%) minmax(0,61%);
  width:min(980px,100%);
  min-height:590px;
  margin:0 auto;
  overflow:hidden;
  border:1px solid rgba(35,67,47,.13);
  border-radius:22px;
  background:#fff;
  box-shadow:0 28px 70px rgba(26,39,30,.11);
}

/* ---------- Dark editorial side ---------- */
.lmwa-c-side{
  display:flex;
  flex-direction:column;
  justify-content:space-between;
  gap:36px;
  min-width:0;
  padding:39px 34px 34px;
  background:
    radial-gradient(circle at 90% 5%,rgba(255,255,255,.11),transparent 29%),
    radial-gradient(circle at 12% 90%,rgba(255,255,255,.06),transparent 28%),
    linear-gradient(155deg,#1d3927 0%,#264932 54%,#315a3d 100%);
  color:#fff;
}

.lmwa-c-kicker,
.lmwa-c-main-kicker{
  display:inline-block;
  color:rgba(255,255,255,.64);
  font-size:8px;
  font-weight:840;
  letter-spacing:.16em;
  text-transform:uppercase;
}

.lmwa-c-side h1{
  margin:14px 0 0;
  color:#fff;
  font-size:clamp(34px,5vw,50px);
  font-weight:710;
  line-height:.98;
  letter-spacing:-.045em;
  text-transform:none;
}

.lmwa-c-side-copy>p{
  max-width:310px;
  margin:18px 0 0;
  color:rgba(255,255,255,.68);
  font-size:12px;
  font-weight:500;
  line-height:1.75;
}

/* account */
.lmwa-c-verified-account{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
  padding:14px 15px;
  border:1px solid rgba(255,255,255,.17);
  border-radius:12px;
  background:rgba(255,255,255,.07);
}
.lmwa-c-verified-account span,
.lmwa-c-verified-account strong{display:block}
.lmwa-c-verified-account span{
  color:rgba(255,255,255,.55);
  font-size:8px;
  font-weight:800;
  letter-spacing:.08em;
  text-transform:uppercase;
}
.lmwa-c-verified-account strong{
  max-width:220px;
  margin-top:5px;
  overflow:hidden;
  color:#fff;
  font-size:11px;
  font-weight:650;
  text-overflow:ellipsis;
  white-space:nowrap;
}

/* progress */
.lmwa-c-progress{
  display:flex;
  align-items:center;
  gap:8px;
}
.lmwa-c-progress>div{
  display:flex;
  min-width:0;
  flex-direction:column;
  gap:5px;
}
.lmwa-c-progress span{
  display:flex;
  width:28px;
  height:28px;
  align-items:center;
  justify-content:center;
  border:1px solid rgba(255,255,255,.25);
  border-radius:50%;
  color:rgba(255,255,255,.66);
  font-size:8px;
  font-weight:800;
}
.lmwa-c-progress strong{
  color:rgba(255,255,255,.58);
  font-size:8px;
  font-weight:720;
}
.lmwa-c-progress .active span{
  border-color:#fff;
  background:#fff;
  color:var(--forest);
}
.lmwa-c-progress .active strong{color:#fff}
.lmwa-c-progress .done span{
  border-color:rgba(255,255,255,.45);
  background:rgba(255,255,255,.14);
  color:#fff;
}
.lmwa-c-progress i{
  width:22px;
  height:1px;
  margin-top:-14px;
  background:rgba(255,255,255,.22);
}

/* ---------- Main ---------- */
.lmwa-c-main{
  min-width:0;
  padding:40px 44px 42px;
  background:#fff;
}

.lmwa-c-main-head{
  padding-bottom:23px;
  border-bottom:1px solid #e5e8e6;
}
.lmwa-c-main-head>span,
.lmwa-c-main-kicker{
  color:var(--forest-2);
  font-size:8px;
  font-weight:840;
  letter-spacing:.14em;
  text-transform:uppercase;
}
.lmwa-c-main-head h2,
.lmwa-shopify-auth h2{
  margin:8px 0 0;
  color:var(--ink);
  font-size:27px;
  font-weight:720;
  line-height:1.15;
  letter-spacing:-.03em;
}
.lmwa-c-main-head p,
.lmwa-shopify-auth p{
  margin:9px 0 0;
  color:#737c75;
  font-size:11px;
  font-weight:500;
  line-height:1.65;
}

/* ---------- Form ---------- */
#customer-form{
  display:grid;
  gap:17px;
  margin-top:25px;
}

#customer-form label{
  display:flex;
  min-width:0;
  flex-direction:column;
  margin:0;
}

#customer-form label>span{
  display:block;
  margin:0 0 7px;
  color:#404b43;
  font-size:10px;
  font-weight:720;
}

#customer-form label>small{
  display:block;
  margin:6px 2px 0;
  color:#8a928c;
  font-size:9px;
  font-weight:500;
  line-height:1.45;
}

#customer-form input,
#customer-form select{
  width:100%;
  height:54px;
  padding:0 14px;
  border:1.5px solid var(--line-strong);
  border-radius:10px;
  outline:none;
  background:#fff;
  color:#202a22;
  box-shadow:0 1px 1px rgba(31,42,34,.015);
  font-size:13px;
  font-weight:600;
  transition:border-color .15s ease,box-shadow .15s ease;
}

#customer-form input::placeholder{color:#9ca49e;font-weight:500}

#customer-form input:hover,
#customer-form select:hover{border-color:#8f9f93}

#customer-form input:focus,
#customer-form select:focus{
  border-color:var(--forest-2);
  box-shadow:0 0 0 3px rgba(48,90,61,.10);
}

#customer-form select{
  -webkit-appearance:menulist!important;
  appearance:auto!important;
  pointer-events:auto!important;
  touch-action:manipulation!important;
  cursor:pointer!important;
}

#customer-form select option{
  background:#fff;
  color:#1e2820;
}

/* note */
.lmwa-c-match-note{
  padding:13px 14px;
  border-left:3px solid var(--forest-2);
  border-radius:0 9px 9px 0;
  background:#f0f4f1;
}
.lmwa-c-match-note strong,
.lmwa-c-match-note span{display:block}
.lmwa-c-match-note strong{
  color:#395140;
  font-size:9px;
  font-weight:800;
  letter-spacing:.04em;
  text-transform:uppercase;
}
.lmwa-c-match-note span{
  margin-top:5px;
  color:#69736c;
  font-size:10px;
  line-height:1.55;
}

/* main CTA */
#a-submit,
.lmwa-shopify-login-button{
  width:100%;
  min-height:54px;
  border:1px solid var(--forest);
  border-radius:10px;
  background:var(--forest);
  color:#fff;
  cursor:pointer;
  box-shadow:0 9px 18px rgba(35,67,47,.14);
  font-size:9px;
  font-weight:840;
  letter-spacing:.10em;
}
#a-submit:hover,
.lmwa-shopify-login-button:hover{background:var(--forest-2)}
#a-submit:disabled{opacity:.6;cursor:not-allowed}

/* messages/result */
.lmwa-c-message{
  margin-top:14px;
  padding:12px 14px;
  border-radius:9px;
  font-size:10px;
  font-weight:560;
  line-height:1.55;
}
.lmwa-c-message.success{border:1px solid #b8cebc;background:var(--success-bg);color:var(--success)}
.lmwa-c-message.error{border:1px solid #dec1bf;background:var(--danger-bg);color:var(--danger)}
.lmwa-c-message.info{border:1px solid #cdd4ce;background:#f1f3f1;color:#566159}

.lmwa-c-result{
  margin-top:16px;
  padding:20px;
  border:1px solid #b9cdbd;
  border-radius:13px;
  background:#edf4ee;
}
.lmwa-c-result .lmwa-c-kicker{color:#4b6d55}
.lmwa-c-result h2{
  margin:8px 0 0;
  color:#25452f;
  font-size:20px;
  font-weight:720;
}
.lmwa-c-result>p{
  margin:7px 0 0;
  color:#647068;
  font-size:10px;
  line-height:1.6;
}

.lmwa-result-grid{
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:1px;
  margin-top:15px;
  overflow:hidden;
  border:1px solid #c6d6c9;
  border-radius:10px;
  background:#c6d6c9;
}
.lmwa-result-grid>div{padding:12px;background:#fff}
.lmwa-result-grid span,
.lmwa-result-grid strong{display:block}
.lmwa-result-grid span{
  color:#818a83;
  font-size:8px;
  font-weight:780;
  text-transform:uppercase;
}
.lmwa-result-grid strong{
  margin-top:5px;
  color:#263129;
  font-size:11px;
  font-weight:680;
  word-break:break-word;
}

/* ---------- Login ---------- */
.lmwa-login-card{min-height:510px}

.lmwa-c-login-note{
  display:flex;
  gap:12px;
  align-items:flex-start;
  padding:14px;
  border:1px solid rgba(255,255,255,.16);
  border-radius:12px;
  background:rgba(255,255,255,.07);
}
.lmwa-c-login-note>span{
  display:flex;
  width:29px;
  height:29px;
  flex:0 0 29px;
  align-items:center;
  justify-content:center;
  border-radius:50%;
  background:#fff;
  color:var(--forest);
  font-size:8px;
  font-weight:820;
}
.lmwa-c-login-note strong,
.lmwa-c-login-note small{display:block}
.lmwa-c-login-note strong{color:#fff;font-size:10px;font-weight:720}
.lmwa-c-login-note small{
  margin-top:4px;
  color:rgba(255,255,255,.58);
  font-size:9px;
  line-height:1.5;
}

.lmwa-c-main-login{
  display:flex;
  align-items:center;
}
.lmwa-shopify-auth{
  width:100%;
}
.lmwa-shopify-auth .lmwa-c-main-kicker{display:block}
#lmwa-customer-account{display:block;margin-top:21px}

/* ---------- Responsive ---------- */
@media(max-width:760px){
  .lmwa-customer{padding:20px 10px 46px}
  .lmwa-c-card{
    grid-template-columns:1fr;
    min-height:0;
    border-radius:16px;
  }
  .lmwa-c-side{
    gap:24px;
    padding:28px 22px 22px;
  }
  .lmwa-c-side h1{
    font-size:36px;
    line-height:1;
  }
  .lmwa-c-side-copy>p{max-width:none}
  .lmwa-c-verified-account{margin-top:0}
  .lmwa-c-progress{margin-top:0}
  .lmwa-c-main{padding:28px 22px 30px}
  .lmwa-c-main-login{min-height:330px}
}

@media(max-width:480px){
  .lmwa-customer{padding:9px 6px 32px}
  .lmwa-c-card{border-radius:13px}
  .lmwa-c-side{padding:24px 16px 19px}
  .lmwa-c-side h1{font-size:32px}
  .lmwa-c-side-copy>p{font-size:11px}
  .lmwa-c-verified-account{padding:12px}
  .lmwa-c-verified-account strong{max-width:210px}
  .lmwa-c-progress{gap:5px}
  .lmwa-c-progress i{width:14px}
  .lmwa-c-progress strong{font-size:7px}
  .lmwa-c-main{padding:24px 15px 26px}
  .lmwa-c-main-head h2,
  .lmwa-shopify-auth h2{font-size:23px}
  #customer-form{gap:15px}
  #customer-form input,
  #customer-form select{
    height:51px;
    font-size:16px;
  }
  .lmwa-result-grid{grid-template-columns:1fr}
}
</style>


<style>
/* V9.1 — CUSTOMER DESKTOP TYPOGRAPHY +2PX */
@media (min-width: 761px){
  .lmwa-customer .lmwa-c-kicker,
  .lmwa-customer .lmwa-c-main-kicker{
    font-size:10px!important;
  }

  .lmwa-customer .lmwa-c-side h1{
    font-size:clamp(36px,5vw,52px)!important;
  }

  .lmwa-customer .lmwa-c-side-copy>p{
    font-size:14px!important;
  }

  .lmwa-customer .lmwa-c-verified-account span{
    font-size:10px!important;
  }

  .lmwa-customer .lmwa-c-verified-account strong{
    font-size:13px!important;
  }

  .lmwa-customer .lmwa-c-progress span{
    font-size:10px!important;
  }

  .lmwa-customer .lmwa-c-progress strong{
    font-size:10px!important;
  }

  .lmwa-customer .lmwa-c-main-head>span{
    font-size:10px!important;
  }

  .lmwa-customer .lmwa-c-main-head h2,
  .lmwa-customer .lmwa-shopify-auth h2{
    font-size:29px!important;
  }

  .lmwa-customer .lmwa-c-main-head p,
  .lmwa-customer .lmwa-shopify-auth p{
    font-size:13px!important;
  }

  .lmwa-customer #customer-form label>span{
    font-size:12px!important;
  }

  .lmwa-customer #customer-form label>small{
    font-size:11px!important;
  }

  .lmwa-customer #customer-form input,
  .lmwa-customer #customer-form select{
    font-size:15px!important;
  }

  .lmwa-customer .lmwa-c-match-note strong{
    font-size:11px!important;
  }

  .lmwa-customer .lmwa-c-match-note span{
    font-size:12px!important;
  }

  .lmwa-customer #a-submit,
  .lmwa-customer .lmwa-shopify-login-button{
    font-size:11px!important;
  }

  .lmwa-customer .lmwa-c-message{
    font-size:12px!important;
  }

  .lmwa-customer .lmwa-c-result h2{
    font-size:22px!important;
  }

  .lmwa-customer .lmwa-c-result>p{
    font-size:12px!important;
  }

  .lmwa-customer .lmwa-result-grid span{
    font-size:10px!important;
  }

  .lmwa-customer .lmwa-result-grid strong{
    font-size:13px!important;
  }

  .lmwa-customer .lmwa-c-login-note strong{
    font-size:12px!important;
  }

  .lmwa-customer .lmwa-c-login-note small{
    font-size:11px!important;
  }
}
</style>
